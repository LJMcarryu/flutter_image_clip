# 0.1.0

- 首次发布 `flutter_image_clip`。
- 提供可直接打开的 `showImageClipEditor` 裁剪 UI API。
- 提供可嵌入页面的 `ImageClipEditor` Widget。
- 提供 `ImageProcessor` 图像处理接口，支持解码、裁剪、旋转、翻转、缩放、调色和 PNG 导出。
- 提供 `ImageClipResult`，返回原图、裁剪图、裁剪区域和旋转角度。
